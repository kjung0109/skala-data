# 조별 보고서 — Sampling Theory 실습

**광주 4반 · [ ]조** | 조원: [ ] | 제출일: 2026-07-27
**선택 주제: 3) 중심극한정리, 4) 정규분포와 이상치 탐지**

선정 이유 — 두 주제가 "반복 평가 → 정규분포 → 이상치 판정"으로 자연스럽게 이어지며, AI 모델 평가·모니터링(AIOps)과 직접 연결되어 조별 토의에서 가장 활발한 논의가 이루어졌기 때문이다.

---

## 1. 중심극한정리 (Central Limit Theorem)

**실습 내용** — 오른쪽으로 크게 치우친 지수분포에서 표본 크기 n=1, 5, 30, 100으로 표본평균을 5,000번씩 뽑아 분포를 비교했다.

**결과**
- n=1: 표본평균 분포가 원자료(지수분포) 그대로 왼쪽에 몰리고 오른쪽 꼬리가 긴 비대칭.
- n=5: 이미 상당히 대칭에 근접.
- n=30·100: 좌우대칭 종 모양(정규분포)으로 수렴하고, 분포의 퍼짐이 크게 좁아짐.

**해석** — 원분포가 어떤 모양이든 표본 크기가 커지면 표본평균의 분포는 정규분포에 수렴한다. 모양은 비대칭 → 대칭 종 모양으로, 퍼짐(표준오차)은 √n에 반비례해 좁아진다.

**AI 연결** — 모델을 서로 다른 시드·샘플로 여러 번 반복 평가한 성능 점수의 평균은, 원 점수 분포가 어떻든 정규분포를 따른다. 덕분에 평균 성능에 신뢰구간을 부여하고("정확도 87% ± 1.5%"), 두 모델의 성능 차이가 통계적으로 유의한지 검정할 수 있다. → 단일 평가값보다 반복 평가의 평균이 신뢰성이 높다.

---

## 2. 정규분포와 이상치 탐지 (Z-score)

**실습 내용** — 평균 200ms, 표준편차 20ms인 응답시간 분포에서 180·215·240·270ms의 z-score를 계산하고, |z|≥2 / |z|≥3 두 기준으로 이상치를 판정했다.

**결과**

| 응답시간 | z-score | \|z\|≥2 | \|z\|≥3 |
|---|---|---|---|
| 180ms | −1.0 | 정상 | 정상 |
| 215ms | +0.75 | 정상 | 정상 |
| 240ms | **+2.0** | **이상치** | 정상 |
| 270ms | +3.5 | 이상치 | 이상치 |

**해석** — 270ms는 두 기준 모두 이상치로 명확하다. 그러나 **240ms(z=2.0)는 기준을 2로 잡으면 이상치, 3으로 잡으면 정상**으로 판정이 갈리는 경계값이다. 임계값을 낮추면 이상치를 더 잡아내지만 거짓 양성(정상을 이상치로 오판)이 늘고, 높이면 놓치는 이상치(거짓 음성)가 늘어난다 — 트레이드오프.

**AI 연결 (AIOps)** — 서버·API 응답시간 모니터링에서 z-score로 지연 급증을 자동 감지할 수 있다. 240ms 같은 경계값은 즉시 장애로 단정하기보다 **"주의(warning)"로 분류**해, 일정 시간 지속되거나 다른 지표(에러율·처리량)와 함께 악화될 때 **"경보(critical)"로 승격**시키는 이중 임계값 방식이 오탐을 줄이면서 실제 장애를 놓치지 않는다.

---

## 3. 조별 토의 — 관점 차이

- **"경계값(240ms)을 이상치로 볼 것인가"** 에서 의견이 갈렸다. 한쪽은 "놓치면 장애로 이어지니 |z|≥2로 민감하게 잡자", 다른 쪽은 "오탐이 잦으면 알람 피로로 진짜 경보를 무시하게 되니 |z|≥3이 낫다"는 입장이었다.
- 이 차이는 **"거짓 양성 비용 vs 거짓 음성 비용 중 무엇을 더 크게 보는가"** 라는 도메인 판단에서 비롯됨을 확인했다. 결제·안전처럼 놓침의 대가가 큰 도메인은 민감하게, 대량 로그 모니터링처럼 오탐 피로가 큰 곳은 보수적으로 잡는 것이 합리적이라는 데 합의했다.
- 두 주제를 관통하는 결론: **중심극한정리가 "평균은 정규분포를 따른다"는 근거를 제공하고, 그 정규분포 위에서 z-score 이상치 판정이 성립**한다. 즉 3번이 4번의 이론적 토대가 된다.
