# 조별 토의 정리 — AI Application (주제 3·4)

**광주 4반 · [ ]조** | 조원: [ ]

---

## Q1. 중심극한정리는 AI 모델의 반복 평가와 어떤 관련이 있는가?

모델을 서로 다른 시드·서로 다른 표본으로 여러 번 반복 평가해 얻은 성능 점수들의 평균은, 원래 점수 분포가 어떤 모양이든 **중심극한정리에 의해 정규분포를 따른다.**

- 덕분에 평균 성능에 대해 **신뢰구간**(예: "정확도 87% ± 1.5%")을 계산할 수 있다.
- 두 모델의 성능 차이가 우연인지, 통계적으로 유의한지 **검정**할 수 있다.
- 단일 평가값은 뽑힌 표본에 따라 크게 흔들리지만, 반복 평가의 평균은 정규분포로 좁게 모이므로 훨씬 신뢰할 수 있는 성능 추정치가 된다.

→ **결론:** "한 번 평가한 점수"보다 "여러 번 평가한 평균 ± 오차"로 보고하는 것이 신뢰성 있는 모델 평가의 기본이다.

---

## Q2. 이상치 탐지가 활용될 수 있는 AI 분야 — 경계값의 오판 중 무엇이 더 치명적인가?

**선택 분야:** 증권 거래 시스템·티켓팅처럼 특정 시각에 트래픽이 몰리는 서비스의 응답시간 모니터링(AIOps)

### 상황 — 그리고 이 예시가 드러내는 함정

이런 시스템에는 동시접속자가 폭증하는 구간이 존재한다.
- 증권: 장 시작(오전 9시) 직후 주문이 한꺼번에 몰림
- 티켓팅: 예매 오픈 순간 수많은 사용자가 동시 접속

여기서 중요한 점은, **이 피크 시간대의 응답 지연은 "이상치"가 아니라 매번 반복되는 예측 가능한 정상 부하**라는 것이다. 그런데 평상시 기준(평균 200ms, |z|≥2)을 그대로 적용하면, 240ms 같은 값이 피크마다 이상치로 잡혀 **거짓 양성(오탐) 알람이 쏟아진다.** 즉 이 사례의 진짜 문제는 "경계값을 이상치로 볼까 정상으로 볼까"가 아니라, **고정 임계값 자체가 트래픽 급증 상황에 맞지 않는다**는 데 있다.

### 어느 실수가 더 치명적인가 — 상황에 따라 다르다

- **평상시(트래픽 안정 구간):** 240ms를 이상치로 판정하는 **거짓 양성**이 더 문제다. 오탐이 쌓이면 알람 피로로 진짜 경보까지 무시하게 된다. → 보수적으로(|z|≥3) 잡는 것이 낫다.
- **피크 구간(장 시작·티켓 오픈):** 240ms를 정상으로 흘려보내는 **거짓 음성**이 더 치명적이다. 이 구간의 지연은 곧 주문 체결 지연·예매 실패로 직결되고, 초기 대응이 늦으면 큐 적체 → 타임아웃 → 장애로 연쇄 악화된다. → 민감하게(|z|≥2) 잡아 오토스케일링·부하 분산을 미리 발동시켜야 한다.

즉 "거짓 양성 비용 vs 거짓 음성 비용" 중 무엇이 큰지가 **시간대에 따라 뒤바뀐다.**

### 조별 합의 — 동적 임계값(baseline)

> 고정된 하나의 임계값으로는 예측 가능한 트래픽 급증을 감당할 수 없다. **시간대별로 기준선(baseline)을 다르게 학습**해, 피크 시간대에는 그 시간대의 평균·표준편차를 기준으로 이상치를 판정하는 **동적 임계값**이 필요하다.

이렇게 하면 피크의 "정상적인 지연"은 오탐으로 잡지 않으면서, 피크 안에서 발생하는 **진짜 비정상(그 시간대 기준으로도 벗어나는 지연)**은 놓치지 않을 수 있다. 단순 z-score 이상치 탐지의 한계와, 그것을 실무에서 어떻게 보완하는지를 보여주는 사례다.
